# Ogata ch4 習題
# Programming Massively Parallel Processors  
## A Hands-on Approach (Fourth Edition)  
### Chapter 4 – Compute Architecture and Scheduling  
### Exercise

---

## Q1  
Consider the following CUDA kernel and the corresponding host function that calls it:

### a. What is the number of warps per block?
- 128 / 32 = **4**

### b. What is the number of warps in the grid?
- 8 × 4 = **32**

### c. For the statement on line 04:

#### I. How many warps in the grid are active?
- 0–31 (O)  
- 32–63 (O)  
- 63–95 (X)  
- 96–127 (O)  

→ 8 × 3 = **24**

#### II. How many warps in the grid are divergent?
- 0–31 (X)  
- 32–63 (O)  
- 63–95 (X)  
- 96–127 (O)  

→ 8 × 2 = **16**

#### III. What is the SIMD efficiency (in %) of warp 0 of block 0?
- **100%**

#### IV. What is the SIMD efficiency (in %) of warp 1 of block 0?
- 8 / 32 = **25%**

#### V. What is the SIMD efficiency (in %) of warp 3 of block 0?
- 24 / 32 = **75%**

---

### d. For the statement on line 07:

#### I. How many warps in the grid are active?
- 4 × 8 = **32**

#### II. How many warps in the grid are divergent?
- **32**

#### III. What is the SIMD efficiency (in %) of warp 0 of block 0?
- **50%**

---

### e. For the loop on line 09:

#### I. How many iterations have no divergence?
- **3**

#### II. How many iterations have divergence?
- **2**

---

## Q2  
For a vector addition, assume:
- Vector length = 2000  
- Each thread computes one element  
- Block size = 512 threads  

**Total threads in grid:**  
- ⌈2000 / 512⌉ × 512 = **2048**

---

## Q3  
For Q2, how many warps diverge due to boundary check?

- Warps 0–31: (X)  
- Warps 32–63: (X)  
- ...  
- Warps 1984–2015: (O)  
- Warps 2016–2047: (X)  

→ **1 divergent warp**

---

## Q4  
Given execution times (µs):  
2.0, 2.3, 3.0, 2.8, 2.4, 1.9, 2.6, 2.9  

- Max time = 3.0  
- Waiting times:  
  - 1.0 + 0.7 + 0 + 0.2 + 0.6 + 1.1 + 0.4 + 0.1 = **4.1**

**Percentage waiting:**  
- 4.1 / (3.0 × 8) = **17.08%**

---

## Q5  
A CUDA programmer claims that using only 32 threads per block allows omitting `__syncthreads()`.

**Answer:**  
This is **not a good idea**.  
- Modern GPUs do not guarantee lockstep execution within a warp. Without __syncthreads(), race conditions may occur if threads read data before it is written. Synchronization should be used whenever cross-thread dependencies exist, and correctness should not rely on warp size.

---

## Q6  
An SM supports:
- Up to 1536 threads  
- Up to 4 blocks  

Which block size yields the most threads per SM?

a. 128  
b. 256  
c. 512  
d. 1024  

**Answer:**  
- **c. 512 threads per block**

---

## Q7  
Device limits:
- 64 blocks per SM  
- 2048 threads per SM  

### Possible assignments and occupancy:

| Case | Configuration | Occupancy |
|----|----|----|
| A | 8 × 128 | 50% |
| B | 16 × 64 | 50% |
| C | 32 × 32 | 50% |
| D | 64 × 32 | 100% |
| E | 32 × 64 | 100% |

**All are possible.**

---

## Q8  
GPU limits:
- 2048 threads per SM  
- 32 blocks per SM  
- 65,536 registers per SM  

### a. 128 threads/block, 30 registers/thread
- Blocks: 2048 / 128 = 16  
- Registers: 30 × 2048 = 61,440  
- **Occupancy: 100%**

### b. 32 threads/block, 29 registers/thread
- Block limit hit (max 32 blocks)  
- Threads: 32 × 32 = 1024  
- **Occupancy: 50% (block limit)**

### c. 256 threads/block, 34 registers/thread
- Register limit exceeded  
- Max threads ≈ 65,536 / 34 ≈ 1927  
- Active blocks ≈ 7  
- Threads = 7 × 256 = 1792  
- **Occupancy ≈ 87.5% (register limit)**

---

## Q9  
A student claims to multiply two 1024 × 1024 matrices using 32 × 32 thread blocks on a GPU with:
- Max 512 threads per block  
- Max 8 blocks per SM  

**Reaction:**  
This is **not feasible**.  
- It seems infeasible because the device specifies a maximum of 512 threads per block, while the user designed a 32×32 thread block, which contains 1024 threads. This exceeds the hardware limit, so the kernel configuration cannot be launched on that GPU.
